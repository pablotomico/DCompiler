#include <stdio.h>
#include <stdlib.h>
#include "symbolTable.h"
#include "scanner.h"
#include "lexAnalizer.h"


int main(int argc, char **argv) {
    lexAnalizer la;
    initLexAnalizer(&la);


    /*symbolTable st;

    initSymbolTable(&st);

    printSymbolTable(st);
    deleteSymbolTable(&st);
    */
    return EXIT_SUCCESS;
}